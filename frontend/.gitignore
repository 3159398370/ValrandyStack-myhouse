# 前端特定忽略规则 - 已整合到根目录.gitignore
# 如需添加前端特定忽略项，请在此文件中添加
# 通用规则已在根目录.gitignore中定义

# 前端特定文件
node_modules/
dist/

# 构建缓存
.parcel-cache/
.next/
.nuxt/

# 环境变量（相对路径）
.env.local
.env.development.local
.env.test.local
.env.production.local